/**
 * Created by jahv on 2017/5/5.
 */
const webpack = require('webpack');
const path = require('path');

module.exports = {
    entry: {
        main: ['./test/App/index', 'webpack/hot/dev-server', `webpack-dev-server/client?http://localhost:3001`]
    },
    output: {
        path: __dirname,
        filename: '[name].js',
        publicPath: "/assets/",
        chunkFilename: '[name].js'
    },
    module: {
        loaders: [{
            test: /\.js$/,
            include: [
                path.resolve(__dirname, "src"),
                path.resolve(__dirname, "test"),
            ],
            loaders: ['babel-loader']
        }]
    },
    resolve: {
        extensions: ['.js']
    },
    plugins: [
        new webpack.DefinePlugin({}),
        new webpack.HotModuleReplacementPlugin(),
        new webpack.NoEmitOnErrorsPlugin()
    ]
};
